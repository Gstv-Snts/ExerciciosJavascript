const deuses = ["Aegir", "Aud", "Balder"];

console.log(`O array deuses tem ${deuses.length} deuses!`);
for (let i = 0; i < deuses.length; i++) {
  console.log(`O deus numero ${i + 1} tem ${deuses[i].length} characteres!`);
}
